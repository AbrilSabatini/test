package org.mock;

import org.mock.persistence.entity.Player;
import org.mock.persistence.repository.ImplPlayerRepository;
import org.mock.service.ImplPlayerService;

public class Main {
    public static void main(String[] args) {
        //ImplPlayerRepository playerRepository = new ImplPlayerRepository();
        // ImplPlayerService playerService = new ImplPlayerService(playerRepository);

        // System.out.println(playerService.findAll());
        // System.out.println(playerService.findPlayerById(5L));

        // playerService.deletePlayer(3L);
        // System.out.println(playerService.findAll());

        // Player player = new Player(5L, "Matías", "Maipú", "Delantero");
        // playerService.savePlayer(player);
        // System.out.println(playerService.findAll());

    }
}